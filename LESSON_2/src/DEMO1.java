public class DEMO1 {
    public static void main(String[] args) {
        int a = 20;

        String result = a > 0 ? "a la so duong" : a < 0 ? "a la so am" : "a la so 0";
        System.out.println(result);
    }
}
